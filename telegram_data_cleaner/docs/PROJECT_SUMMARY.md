# خلاصه کامل پروژه Telegram Data Cleaner

## 📋 فهرست مطالب
1. [معرفی پروژه](#معرفی-پروژه)
2. [معماری سیستم](#معماری-سیستم)
3. [ویژگی‌های اصلی](#ویژگی‌های-اصلی)
4. [تاریخچه توسعه](#تاریخچه-توسعه)
5. [ساختار پروژه](#ساختار-پروژه)
6. [تکنولوژی‌های استفاده شده](#تکنولوژی‌های-استفاده-شده)

---

## 🎯 معرفی پروژه

**Telegram Data Cleaner** یک سیستم real-time برای تجزیه و تحلیل داده‌های کانال‌های تلگرام است که به شما امکان می‌دهد:

- پیام‌های کانال‌های تلگرام را به صورت خودکار دریافت و ذخیره کنید
- کلمات کلیدی خاص (نمادهای بورسی، صنایع، و غیره) را در پیام‌ها شناسایی کنید
- آنالیتیکس جامع از محتوای هر کانال داشته باشید
- گزارش‌های Excel از آمار تولید کنید

### مشخصات کلی
- **نوع پروژه**: Real-time Data Analysis System
- **زبان برنامه‌نویسی**: Python 3.11+
- **فریمورک**: FastAPI
- **دیتابیس**: PostgreSQL
- **Cache**: Redis
- **UI**: Alpine.js + TailwindCSS

---

## 🏗️ معماری سیستم

### کامپوننت‌های اصلی

```
┌─────────────────────────────────────────────────────┐
│                   Frontend (UI)                     │
│  - Alpine.js + TailwindCSS                          │
│  - Chart.js برای نمودارها                          │
└─────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────┐
│                FastAPI Backend                      │
│  ┌────────────────┬──────────────┬─────────────┐   │
│  │  Ingestion     │  Dictionary  │  Analytics  │   │
│  │  Routes        │  Routes      │  Routes     │   │
│  └────────────────┴──────────────┴─────────────┘   │
└─────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────┐
│              Core Services Layer                    │
│  ┌────────────────────────────────────────────┐    │
│  │  - IngestionService: دریافت پیام‌ها       │    │
│  │  - MatchingService: تطبیق با لغت‌نامه     │    │
│  │  - AnalyticsService: محاسبه آمار          │    │
│  │  - SchedulerService: زمان‌بندی وظایف      │    │
│  └────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────┐
│                 Data Layer                          │
│  ┌──────────────┬──────────────┐                   │
│  │  PostgreSQL  │    Redis     │                   │
│  │  (Main DB)   │   (Cache)    │                   │
│  └──────────────┴──────────────┘                   │
└─────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────┐
│            External API                             │
│  - Telegram Messages API                            │
│    (http://103.75.197.239:3000/api/all-messages)    │
└─────────────────────────────────────────────────────┘
```

### Flow دریافت و پردازش داده

```
1. Scheduler Job (هر 3 دقیقه)
   ↓
2. IngestionService.ingest_batch()
   ↓
3. دریافت پیام‌های جدید از API
   ↓
4. TextNormalizer.normalize() - نرمال‌سازی متن
   ↓
5. ذخیره در دیتابیس
   ↓
6. MatchingService.match_messages() - تطبیق با لغت‌نامه
   ↓
7. ذخیره نتایج تطابق
   ↓
8. AnalyticsService.compute_aggregates() - محاسبه آمار (هر 5 دقیقه)
```

---

## ✨ ویژگی‌های اصلی

### 1. سیستم Ingestion (دریافت پیام)
- **دریافت خودکار**: هر 3 دقیقه پیام‌های جدید دریافت می‌شود
- **Batch Processing**: پردازش دسته‌ای با قابلیت تنظیم تعداد
- **Resume Support**: قابلیت ادامه از آخرین پیام دریافت شده
- **Error Handling**: مدیریت خطا و retry mechanism

### 2. سیستم Dictionary (لغت‌نامه)
- **دسته‌بندی**: لغت‌نامه‌ها به دسته‌ها تقسیم می‌شوند (نمادها، صنایع، و غیره)
- **کلمات کلیدی**: هر کلمه می‌تواند کلمات کلیدی اضافی داشته باشد
- **Extra Data**: اطلاعات اضافی مثل نام شرکت، نام صنعت در JSONB
- **Hashtag Support**: پشتیبانی از هشتگ‌ها (#نماد)

#### مثال ساختار لغت‌نامه:
```json
{
  "word": "فولاد",
  "category": "نمادها",
  "extra_data": {
    "symbol_name": "فولاد",
    "company_name": "فولاد مبارکه اصفهان",
    "industry_name": "فلزات اساسی",
    "keywords": ["#فولاد", "فولادمبارکه"]
  }
}
```

### 3. سیستم Matching (تطبیق)
- **نرمال‌سازی متن**: تبدیل حروف عربی به فارسی، حذف diacritics
- **Hashtag Processing**: تبدیل #نماد به نماد برای تطابق
- **Keyword Matching**: تطبیق با کلمات کلیدی
- **Real-time**: بلافاصله پس از دریافت پیام

#### تغییرات مهم در Matching:
قبلاً:
```python
text = re.sub(r'#\w+', '', text)  # حذف کامل هشتگ
```

بعد از اصلاح:
```python
text = re.sub(r'#(\w+)', r'\1', text)  # نگه‌داشتن متن، حذف #
```

### 4. سیستم Analytics (آنالیتیکس)
- **Real-time Stats**: آمار لحظه‌ای (5، 30، 60 دقیقه)
- **Aggregated Data**: داده‌های از پیش محاسبه شده (روزانه/ساعتی)
- **Top Lists**: 10 نماد، صنعت، و دسته‌بندی برتر
- **Content Profile**: تحلیل تمرکز محتوایی کانال
- **Excel Export**: خروجی گزارش به صورت Excel

#### جدول Analytics:
```sql
CREATE TABLE channel_analytics (
  id UUID PRIMARY KEY,
  channel_id UUID NOT NULL,
  date DATE NOT NULL,
  hour INTEGER,  -- NULL برای daily
  day_of_week INTEGER,
  message_count INTEGER DEFAULT 0,
  match_count INTEGER DEFAULT 0,
  top_symbols JSONB,
  top_industries JSONB,
  top_categories JSONB,
  created_at TIMESTAMP,
  updated_at TIMESTAMP,
  UNIQUE(channel_id, date, hour)
);
```

---

## 📅 تاریخچه توسعه

### فاز 1: پایه‌گذاری (روزهای اول)
- ✅ طراحی معماری اولیه
- ✅ ایجاد مدل‌های دیتابیس (Channel, Message, Category)
- ✅ سیستم Ingestion پایه
- ✅ UI اولیه با Alpine.js

### فاز 2: سیستم لغت‌نامه (روزهای میانی)
- ✅ ایجاد مدل‌های Dictionary
- ✅ سیستم دسته‌بندی
- ✅ API های CRUD برای لغت‌نامه
- ✅ Import از CSV
- ✅ UI مدیریت لغت‌نامه

### فاز 3: سیستم Matching
- ✅ TextNormalizer برای پردازش متن فارسی
- ✅ Hazm integration با fallback
- ✅ MatchingService
- ✅ صفحه نمایش تطابق‌ها

### فاز 4: پشتیبانی از Hashtag
**مشکل اصلی**: پیام‌هایی که #نماد داشتند، تطابق پیدا نمی‌کردند

**راه‌حل**:
1. اضافه کردن `#نماد` به keywords تمام نمادها
2. تغییر TextNormalizer برای نگه‌داشتن متن هشتگ
3. Re-normalize کردن پیام‌های قدیمی (اختیاری)

**کد تغییر یافته**:
```python
# قبل
text = re.sub(r'#\w+', '', text)

# بعد
text = re.sub(r'#(\w+)', r'\1', text)
```

**اسکریپت اضافه کردن هشتگ**:
```python
# /tmp/add_hashtag_to_symbols.py
for symbol in symbols:
    hashtag = f"#{symbol.word}"
    if hashtag not in symbol.extra_data['keywords']:
        symbol.extra_data['keywords'].append(hashtag)
        flag_modified(symbol, 'extra_data')
```

### فاز 5: سیستم Analytics (آخرین فاز - امروز)
- ✅ مدل ChannelAnalytics
- ✅ ChannelAnalyticsService
- ✅ Background job برای aggregation (هر 5 دقیقه)
- ✅ API endpoints کامل
- ✅ Excel export با openpyxl
- ✅ UI Analytics با Chart.js
- ✅ راهنمای استفاده در UI

---

## 📁 ساختار پروژه

```
telegram_data_cleaner/
├── alembic/                      # Database migrations
│   └── versions/
│       ├── *_initial_tables.py
│       ├── *_add_dictionary_system_models.py
│       ├── *_add_extra_data_field.py
│       └── *_add_channel_analytics_table.py
│
├── docs/                         # مستندات (جدید)
│   ├── PROJECT_SUMMARY.md        # این فایل
│   ├── API_GUIDE.md              # راهنمای API
│   ├── ANALYTICS_GUIDE.md        # راهنمای Analytics
│   └── SESSION_NOTES.md          # یادداشت‌های جلسه
│
├── src/
│   ├── api/
│   │   └── routes/
│   │       ├── ingestion.py      # API دریافت پیام
│   │       ├── dictionary.py     # API لغت‌نامه
│   │       ├── scheduler.py      # API زمان‌بندی
│   │       └── analytics.py      # API آنالیتیکس ⭐ جدید
│   │
│   ├── core/
│   │   ├── analytics/
│   │   │   └── channel_analytics_service.py  ⭐ جدید
│   │   ├── ingestion/
│   │   │   └── telegram_api_client.py
│   │   ├── matching/
│   │   │   └── matching_service.py
│   │   └── processing/
│   │       └── text_normalizer.py  # اصلاح شده برای هشتگ
│   │
│   ├── models/
│   │   ├── channel.py
│   │   ├── message.py
│   │   ├── dictionary.py
│   │   ├── dictionary_category.py
│   │   ├── dictionary_word.py
│   │   ├── message_dictionary.py
│   │   └── channel_analytics.py   ⭐ جدید
│   │
│   ├── services/
│   │   ├── ingestion_service.py
│   │   └── scheduler_service.py   # اضافه شدن analytics job
│   │
│   ├── templates/
│   │   ├── index.html             # لغت‌نامه
│   │   ├── dashboard.html         # داشبورد
│   │   ├── matches.html           # تطابق‌ها
│   │   └── analytics.html         ⭐ جدید
│   │
│   ├── config.py
│   ├── database.py
│   └── main.py
│
├── scripts/                       # اسکریپت‌های کمکی
├── tests/
├── pyproject.toml
└── README.md
```

---

## 🔧 تکنولوژی‌های استفاده شده

### Backend
| تکنولوژی | نسخه | استفاده |
|----------|------|----------|
| Python | 3.11+ | زبان اصلی |
| FastAPI | Latest | Web framework |
| SQLAlchemy | 2.x | ORM |
| Alembic | Latest | Database migrations |
| PostgreSQL | Latest | Database |
| Redis | Latest | Cache |
| APScheduler | Latest | Job scheduling |
| Pydantic | 2.x | Validation |
| openpyxl | 3.1.5 | Excel export |

### Frontend
| تکنولوژی | استفاده |
|----------|----------|
| Alpine.js | Reactive UI |
| TailwindCSS | Styling |
| Chart.js | نمودارها |
| Font Awesome | آیکون‌ها |
| Vazirmatn | فونت فارسی |

### Text Processing
| تکنولوژی | استفاده |
|----------|----------|
| Hazm | پردازش متن فارسی (اختیاری) |
| Regex | Pattern matching |

---

## 🗂️ مدل‌های دیتابیس

### جداول اصلی

#### 1. channels
```sql
- id (UUID, PK)
- telegram_id (String, Unique)
- name (String)
- username (String)
- category_id (UUID, FK)
- is_active (Boolean)
- last_sync (DateTime)
```

#### 2. messages
```sql
- id (UUID, PK)
- channel_id (UUID, FK)
- message_id (String)
- text (Text)
- text_normalized (Text)  -- متن نرمال شده
- created_at (DateTime)
```

#### 3. dictionary_categories
```sql
- id (UUID, PK)
- name (String)  -- مثلاً "نمادها"
- description (Text)
```

#### 4. dictionary_words
```sql
- id (UUID, PK)
- category_id (UUID, FK)
- word (String)
- normalized_word (String)
- is_active (Boolean)
- extra_data (JSONB)  -- keywords, company_name, etc.
```

#### 5. message_dictionary (رابطه many-to-many)
```sql
- id (UUID, PK)
- message_id (UUID, FK)
- word_id (UUID, FK)
- matched_text (String)  -- متن دقیق که match شده
```

#### 6. channel_analytics ⭐ جدید
```sql
- id (UUID, PK)
- channel_id (UUID, FK)
- date (Date)
- hour (Integer, nullable)
- day_of_week (Integer)
- message_count (Integer)
- match_count (Integer)
- top_symbols (JSONB)
- top_industries (JSONB)
- top_categories (JSONB)
- created_at, updated_at
- UNIQUE(channel_id, date, hour)
```

---

## 🔄 Job های زمان‌بند

| Job | فاصله | توضیح |
|-----|-------|--------|
| **Ingestion Job** | هر 3 دقیقه | دریافت پیام‌های جدید |
| **Cleanup Job** | روزانه ساعت 2 صبح | حذف پیام‌های قدیمی |
| **Health Check Job** | هر 5 دقیقه | بررسی سلامت سیستم |
| **Analytics Aggregation Job** ⭐ | هر 5 دقیقه | محاسبه aggregates |

---

## 📊 API Endpoints

### Ingestion
- `POST /api/ingestion/sync` - دریافت دستی پیام‌ها
- `GET /api/ingestion/channels` - لیست کانال‌ها
- `DELETE /api/ingestion/cleanup` - پاک‌سازی

### Dictionary
- `GET /api/dictionary/categories` - لیست دسته‌ها
- `POST /api/dictionary/categories` - ایجاد دسته جدید
- `GET /api/dictionary/words` - لیست کلمات
- `POST /api/dictionary/words` - اضافه کردن کلمه
- `PATCH /api/dictionary/words/{id}` - ویرایش کلمه
- `DELETE /api/dictionary/words/{id}` - حذف کلمه
- `POST /api/dictionary/import/csv` - Import از CSV

### Analytics ⭐ جدید
- `GET /api/analytics/channels/{id}/stats` - آمار کانال
- `GET /api/analytics/channels/{id}/content-profile` - پروفایل محتوا
- `GET /api/analytics/channels/compare` - مقایسه کانال‌ها
- `GET /api/analytics/channels/{id}/export/excel` - خروجی Excel
- `POST /api/analytics/compute-aggregates` - محاسبه دستی

---

## 🎓 نکات مهم برای ادامه پروژه

### 1. مشکل Generic Words (مطرح شده، حل نشده)
**مشکل**: برخی کلمات خیلی عمومی هستند (مثل "بورس"، "سلام")

**راه‌حل‌های پیشنهادی**:
1. Stopwords List
2. TF-IDF Filtering
3. Document Frequency Threshold
4. Context-aware Matching
5. Whitelisting برای نمادها

**وضعیت**: کاربر تصمیم گرفت فعلاً skip شود

### 2. Re-processing پیام‌های قدیمی
پیام‌های قدیمی که قبل از اصلاح TextNormalizer دریافت شده‌اند، هشتگ‌ها را match نمی‌کنند.

**راه‌حل**:
- بگذارید طبیعتاً expire شوند (با history_days)
- یا re-normalize شوند با اسکریپت

**تصمیم**: skip شد

### 3. UI Analytics
صفحه analytics آماده است اما نیاز به تست دارد.

### 4. Performance
- برای کانال‌های با حجم بالا، caching بیشتر
- Index optimization
- Query optimization

---

## 🚀 نحوه اجرا

### 1. نصب Dependencies
```bash
poetry install
```

### 2. تنظیم Environment Variables
```bash
cp .env.example .env
# ویرایش .env
```

### 3. اجرای Migrations
```bash
poetry run alembic upgrade head
```

### 4. اجرای سرور
```bash
poetry run python -m uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload
```

### 5. دسترسی به UI
- خانه (لغت‌نامه): http://localhost:8000/
- داشبورد: http://localhost:8000/dashboard
- تطابق‌ها: http://localhost:8000/matches
- آنالیتیکس: http://localhost:8000/analytics ⭐

---

## 📝 TODO List

### فوری
- [ ] تست کامل Analytics API
- [ ] تست Excel export
- [ ] تست UI Analytics
- [ ] اضافه کردن نمونه داده برای دموی Analytics

### آینده
- [ ] حل مشکل Generic Words
- [ ] بهینه‌سازی Performance
- [ ] اضافه کردن Authentication
- [ ] Webhook support برای real-time updates
- [ ] Mobile responsive بهتر
- [ ] Dark mode
- [ ] Multi-language support

---

## 👥 نکات برای ارائه به تیم

### نقاط قوت پروژه
1. ✅ معماری تمیز و قابل توسعه
2. ✅ Background jobs برای automation
3. ✅ UI کاربرپسند با Alpine.js
4. ✅ Support از متن فارسی
5. ✅ Analytics جامع و کاربردی
6. ✅ Excel export برای reporting

### چالش‌های حل شده
1. ✅ Hashtag matching
2. ✅ Persian text normalization
3. ✅ JSONB updates در SQLAlchemy
4. ✅ Real-time vs aggregated data

### چالش‌های باقی‌مانده
1. ⏳ Generic words filtering
2. ⏳ Performance optimization
3. ⏳ Authentication & Authorization

---

**تاریخ آخرین به‌روزرسانی**: 2025-11-16
**وضعیت پروژه**: ✅ آماده برای دمو و تست
**نسخه**: 0.2.0
