# 📚 راهنمای کامل استفاده از سیستم لغت‌نامه

## 🌐 دسترسی به رابط کاربری

**آدرس:** http://localhost:8000/docs

این صفحه به صورت خودکار تمام API های موجود رو نمایش می‌ده و می‌تونی مستقیماً از توی مرورگر تست کنی.

---

## 📖 مراحل کار با لغت‌نامه (گام به گام)

### مرحله 1️⃣: ساخت لغت‌نامه جدید

1. برو به آدرس: http://localhost:8000/docs
2. دنبال بخش **"Dictionary Management"** بگرد
3. روی **POST /api/dictionary/dictionaries** کلیک کن
4. روی دکمه **"Try it out"** کلیک کن
5. متن زیر رو توی قسمت Request body بنویس:

```json
{
  "name": "بورس اوراق بهادار",
  "description": "نمادهای بورسی ایران",
  "is_active": true
}
```

6. روی دکمه **"Execute"** کلیک کن
7. پایین صفحه، توی قسمت Response، یه چیزی شبیه این می‌بینی:

```json
{
  "id": "abe10651-3b61-440b-a8b6-aa8b56449c07",
  "name": "بورس اوراق بهادار",
  "description": "نمادهای بورسی ایران",
  "is_active": true,
  "created_at": "2025-11-15T12:00:00Z",
  "updated_at": "2025-11-15T12:00:00Z"
}
```

**⚠️ مهم:** این `id` رو یه جایی کپی کن، برای مرحله بعد لازمش داری!

---

### مرحله 2️⃣: ساخت دسته‌بندی

1. توی همون صفحه، دنبال **POST /api/dictionary/categories** بگرد
2. روی **"Try it out"** کلیک کن
3. این رو بنویس (توجه: `dictionary_id` همون ID ای که قبلاً کپی کردی):

```json
{
  "dictionary_id": "abe10651-3b61-440b-a8b6-aa8b56449c07",
  "name": "نمادها",
  "description": "نمادهای معاملاتی بورس"
}
```

4. روی **"Execute"** کلیک کن
5. دوباره یه `id` بهت می‌ده، این یکی هم کپی کن (برای مرحله 3 لازمه):

```json
{
  "id": "660459ef-4060-4bd8-bc4b-c1a4d2c176e0",
  "dictionary_id": "abe10651-3b61-440b-a8b6-aa8b56449c07",
  "name": "نمادها",
  ...
}
```

---

### مرحله 3️⃣: اضافه کردن کلمات (نمادها)

#### روش A: اضافه کردن یک نماد با اطلاعات کامل

1. دنبال **POST /api/dictionary/words** بگرد
2. روی **"Try it out"** کلیک کن
3. این رو بنویس (توجه: `category_id` همون ID دسته‌بندی قبلیه):

```json
{
  "category_id": "660459ef-4060-4bd8-bc4b-c1a4d2c176e0",
  "word": "فولاد",
  "is_active": true,
  "extra_data": {
    "symbol_name": "فولاد",
    "company_name": "فولاد مبارکه اصفهان",
    "industry_name": "فلزات اساسی",
    "company_code": "IRO1FOLD0001"
  }
}
```

4. روی **"Execute"** کلیک کن

---

#### روش B: اضافه کردن چند نماد یکجا (بدون اطلاعات اضافی)

1. دنبال **POST /api/dictionary/words/bulk** بگرد
2. روی **"Try it out"** کلیک کن
3. این رو بنویس:

```json
{
  "category_id": "660459ef-4060-4bd8-bc4b-c1a4d2c176e0",
  "words": [
    "شپنا",
    "خودرو",
    "وبملت",
    "فملی",
    "شبندر"
  ],
  "is_active": true
}
```

4. روی **"Execute"** کلیک کن

**نکته:** با این روش نمی‌تونی اطلاعات اضافی (مثل نام شرکت) بدی، فقط نام نماد اضافه میشه.

---

### مرحله 4️⃣: مشاهده لیست نمادها

1. دنبال **GET /api/dictionary/words** بگرد
2. روی **"Try it out"** کلیک کن
3. توی قسمت **category_id**، ID دسته‌بندی رو بنویس
4. روی **"Execute"** کلیک کن
5. لیست همه نمادهای اون دسته رو می‌بینی

---

## 🔍 مثال کامل: ساخت لغت‌نامه نمادهای بورس

### گام 1: ساخت لغت‌نامه
```
POST /api/dictionary/dictionaries
```
```json
{
  "name": "لغت‌نامه بورس",
  "description": "تمام نمادها و شرکت‌های بورسی",
  "is_active": true
}
```
**نتیجه:** `dictionary_id` = `abc-123-def`

---

### گام 2: ساخت دسته برای گروه فلزات
```
POST /api/dictionary/categories
```
```json
{
  "dictionary_id": "abc-123-def",
  "name": "فلزات اساسی",
  "description": "نمادهای صنعت فلزات"
}
```
**نتیجه:** `category_id` = `xyz-456-uvw`

---

### گام 3: اضافه کردن نمادهای فلزات
```
POST /api/dictionary/words
```

**نماد 1: فولاد**
```json
{
  "category_id": "xyz-456-uvw",
  "word": "فولاد",
  "is_active": true,
  "extra_data": {
    "symbol_name": "فولاد",
    "company_name": "فولاد مبارکه اصفهان",
    "industry_name": "فلزات اساسی",
    "company_code": "IRO1FOLD0001"
  }
}
```

**نماد 2: فملی**
```json
{
  "category_id": "xyz-456-uvw",
  "word": "فملی",
  "is_active": true,
  "extra_data": {
    "symbol_name": "فملی",
    "company_name": "ملی صنایع مس ایران",
    "industry_name": "فلزات اساسی",
    "company_code": "IRO1MSMI0001"
  }
}
```

---

### گام 4: ساخت دسته دیگه برای گروه خودرو
```
POST /api/dictionary/categories
```
```json
{
  "dictionary_id": "abc-123-def",
  "name": "خودروسازی",
  "description": "نمادهای صنعت خودرو"
}
```
**نتیجه:** `category_id` = `qrs-789-tuv`

---

### گام 5: اضافه کردن نمادهای خودرو
```
POST /api/dictionary/words/bulk
```
```json
{
  "category_id": "qrs-789-tuv",
  "words": ["خودرو", "خساپا", "خریز", "خپارس"],
  "is_active": true
}
```

---

## 📊 دستورات مفید دیگر

### مشاهده همه لغت‌نامه‌ها
```
GET /api/dictionary/dictionaries
```
فیلتر فقط فعال‌ها:
```
GET /api/dictionary/dictionaries?active_only=true
```

---

### مشاهده همه دسته‌بندی‌های یک لغت‌نامه
```
GET /api/dictionary/categories?dictionary_id=abc-123-def
```

---

### مشاهده آمار کل سیستم
```
GET /api/dictionary/stats
```
نتیجه:
```json
{
  "total_dictionaries": 3,
  "active_dictionaries": 3,
  "total_categories": 6,
  "total_words": 45,
  "active_words": 42,
  "total_matches": 128
}
```

---

### غیرفعال کردن یک نماد (بدون حذف)
```
PATCH /api/dictionary/words/{word_id}
```
```json
{
  "is_active": false
}
```

---

### حذف کامل یک نماد
```
DELETE /api/dictionary/words/{word_id}
```

---

## 💡 نکات مهم

1. **ID ها رو کپی کن**: هر بار که چیزی می‌سازی، ID اش رو یه جایی نگه دار

2. **ترتیب مهمه**: 
   - اول لغت‌نامه
   - بعد دسته‌بندی
   - آخر کلمات

3. **extra_data اختیاریه**: می‌تونی بدون اطلاعات اضافی هم کلمه اضافه کنی

4. **Bulk برای سرعت**: اگه کلمات زیادی داری، از `/words/bulk` استفاده کن

5. **غیرفعال کردن بهتر از حذف**: بجای delete، `is_active: false` کن تا بعداً بتونی دوباره فعالش کنی

---

## 🎯 ساختار پیشنهادی برای بورس

```
لغت‌نامه: بورس اوراق بهادار
│
├─ دسته: فلزات اساسی
│  ├─ فولاد (شرکت: فولاد مبارکه)
│  ├─ فملی (شرکت: ملی صنایع مس)
│  └─ فخوز (شرکت: خوزستان فولاد)
│
├─ دسته: خودروسازی  
│  ├─ خودرو (شرکت: ایران خودرو)
│  ├─ خساپا (شرکت: سایپا)
│  └─ خپارس (شرکت: پارس خودرو)
│
├─ دسته: بانکداری
│  ├─ وبملت (شرکت: بانک ملت)
│  ├─ وپاسار (شرکت: پاسارگاد)
│  └─ وتجارت (شرکت: تجارت)
│
└─ دسته: پالایشگاه
   ├─ شپنا (شرکت: پالایش نفت اصفهان)
   ├─ شبندر (شرکت: پالایش نفت بندرعباس)
   └─ شتران (شرکت: پالایش نفت تهران)
```

---

## ❓ سوالات متداول

**Q: چطور یه نماد رو ویرایش کنم؟**
A: از `PATCH /api/dictionary/words/{word_id}` استفاده کن

**Q: می‌شه دسته‌بندی توی تو داشت؟**
A: بله! وقتی دسته می‌سازی، `parent_id` رو مشخص کن

**Q: چطور فهمیدم یه پیام کدوم نمادها رو داره؟**
A: سیستم خودکار matching انجام می‌ده، توی جدول `message_dictionaries` ذخیره میشه

**Q: می‌تونم اطلاعات دیگه به extra_data اضافه کنم؟**
A: بله! هر چیزی که بخوای، مثلاً: `market_cap`, `pe_ratio`, `website`, etc.

---

## 🚀 شروع سریع

اگه می‌خوای سریع شروع کنی، این کدها رو توی Swagger بزن:

### 1. بساز لغت‌نامه:
```json
POST /api/dictionary/dictionaries
{"name": "بورس", "description": "نمادهای بورس", "is_active": true}
```

### 2. بساز دسته:
```json
POST /api/dictionary/categories
{"dictionary_id": "ID-از-مرحله-قبل", "name": "نمادها", "description": "همه نمادها"}
```

### 3. اضافه کن نمادها:
```json
POST /api/dictionary/words/bulk
{"category_id": "ID-دسته", "words": ["فولاد","شپنا","خودرو"], "is_active": true}
```

**تمام!** 🎉

---

برای کمک بیشتر:
- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc
